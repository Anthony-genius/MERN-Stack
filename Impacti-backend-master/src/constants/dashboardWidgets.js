const keyMirror = require('keymirror');

const DASHBOARD_WIDGETS = keyMirror({
  JOURNEY_PROGRESS: null,
  LATEST_JOURNEY_REPORTS: null,
  LATEST_ACHIEVEMENTS: null,
  COMMITMENT_TRACKER: null,
});

module.exports = DASHBOARD_WIDGETS;

